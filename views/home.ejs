<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Library Management</title>
</head>
<body>
    <h1>All Books!</h1>
    <button><a href="/api/books/new">Add New Book</a></button>
    <% for(let book of books) {%>
        <div>
            <li> <%= book.title %></li>
            <a href="/api/books/<%= book._id %>">Edit</a>
        </div>
    <% } %>
</body>
</html>